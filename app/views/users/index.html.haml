%h1.column.width4.first All users
%section.width4.first
  %ul
    - @users.each do |user|
      %li!= link_to user.name, user
  %p NOTE: This would be a smash place to show the most bestest users, the latest new users, whatever.
  