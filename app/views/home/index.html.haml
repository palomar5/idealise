%section.width4.first
  %h1.column.width2.first.claim
    Because you love
    %br
    getting it right.
  .column.width2.intro
    %p Get instant feedback for your project.
    %p Build your reputation as a trusted advisor.
    %p Discover the smartest teams in the world building the Next Big Thing.
%section.column.width2.first
  %h2 Hot projects
  %table
    %tbody
      - @hot_projects.each do |project|
        %tr
          %td
            !=link_to h(project.title), project
          %td
            != kudos_sparkline_tag(project.kudos_history, cycle("f6f6f6", "ffffff")) unless project.kudos_history.empty?
  
%section.column.width2
  %h2 Active users
  %table
    %tbody
      - @respected_users.each do |user|
        %tr
          %td
            !=link_to h(user.name), user
          %td
            Helpfulness:
            != user.rating_points
  